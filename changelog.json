{
  "_comment": "The changelog. tools/changelog.py renders CHANGELOG.md from this file, and the plugin shows the same entries in game under What's new (the file is embedded in Almanac.Core). Every user-visible change edits this file in the same commit. Statuses: new/fix (in a release, seen working), beta (merged, not yet verified in its real setting), next (still being built).",
  "project": "Almanac",
  "intro": "Every change to Almanac a user can see, newest first: the local agent and its engine, and the Almanac plugin in FFXIV.\n\nAlmanac has not had a release yet. Everything below is merged and unit-tested with fakes; what has and has not actually been run against a real model server, a real game or real hardware is listed in the README's \"Untested\" section, and each entry says so for itself.",
  "releases": [
    {
      "version": "next",
      "title": "In the workshop",
      "blurb": "Merged, but nothing here has been through a release. BETA means it is built and tested with fakes but has not been seen working in the place it is meant to run; SOON means it is still being built.",
      "items": [
        {
          "status": "beta",
          "text": "Autopilot: a task queue and loop that picks work up from GitHub issues, the inbox, vote tallies and failing CI, plans it with a pool of local models (roles, health checks and failover), and runs coding sessions in sandboxed git worktrees with a hybrid cloud/local coder that falls back to a local model when the cloud hits its limit. Budget caps, a kill switch, a durable log and a morning digest come with it. Nothing here has run against a real coding CLI, a real model backend or a real pull request yet."
        },
        {
          "status": "beta",
          "text": "Nothing autopilot writes leaves its sandbox without you: each change waits on an approval request you see in game, deny paths and caps are enforced before a session starts, and `almanac autopilot pending` lists what is parked. Not yet exercised against XivMcp's real approval tickets."
        },
        {
          "status": "beta",
          "text": "Install Almanac in the game without a .NET SDK: it is published as a normal Dalamud plugin from the author's repository at spacegho.st/mods/ffxiv/plugins.json. A `v*` tag builds, tests, packages and publishes it on GitHub's own runners, and a pre-release tag goes to the testing channel instead. Building it yourself still works. No release has been cut yet."
        },
        {
          "status": "beta",
          "text": "The plugin download is only what the game loads — the managed assemblies, the manifest and the one native SQLite for win-x64 — instead of the 49 MB the first packaging run produced; the build now fails if the manifest or the plugin assembly is missing from the zip."
        },
        {
          "status": "beta",
          "text": "Almanac in game: a Dalamud plugin with a setup wizard that finds your local model server, reads the GPU through DXGI, subtracts what the game needs and recommends models for the VRAM that is left. Not yet run in a real game: the windows, DXGI detection under Wine/DXVK and the SQLite native library inside Dalamud are all unverified."
        },
        {
          "status": "beta",
          "text": "Chat window with conversation threads, branching and replies streamed as they are written, running the agent loop against XivMcp's tools. Not yet run in game."
        },
        {
          "status": "beta",
          "text": "XivMcp is found over Dalamud IPC and hands Almanac its own client token, so no token is copied or stored here, and tools that change the game still go through XivMcp's in-game approval. Almanac's own `Almanac.Ask` IPC lets another plugin ask a question — this is what a terminal's /ask uses. Not yet run in game."
        },
        {
          "status": "beta",
          "text": "Benchmark window: run the ffxiv-core suite against your own model server, see the score and past runs, and share a result with the community leaderboard only when you press Share. Live benchmark mode has not been run in game."
        },
        {
          "status": "beta",
          "text": "Almanac.Core carries the whole engine with no game and no Python in it: a streaming OpenAI-compatible chat client with a prompted tool-calling fallback for models that lack native tool calls, an MCP client for XivMcp, an agent loop that validates every tool call against its schema, local server detection, VRAM-tiered model recommendations, the benchmark runner and scorer, and one SQLite file for settings, threads, runs and caches. 70 tests with fakes and no network."
        },
        {
          "status": "beta",
          "text": "`almanac bench`: headless benchmark runs of the ffxiv-core suite against any OpenAI-compatible backend, with mock fixtures or live XivMcp tools, a prompted fallback when the backend refuses tools, peak VRAM sampled, every run kept in bench.sqlite, and a result document submitted only on request. Not yet run against a real model server or live XivMcp."
        },
        {
          "status": "beta",
          "text": "Conversation threads and machine-readable streaming for the CLI: `ask` and `chat` take --thread, --continue or --new-thread, `ask --stream-json` prints one JSON object per line and never prompts (for programs such as an in-game panel), and `almanac threads` lists, shows and deletes them."
        },
        {
          "status": "beta",
          "text": "Model residency: while a process you name is running, the gateway loads the model and keeps it pinned in VRAM, re-asserting the pin after a backend restart, and restores the idle timeout when the process exits. Pins respect the memory guard — they are deferred, not failed — and the state shows in /healthz and through the model_residency tool."
        },
        {
          "status": "beta",
          "text": "The model gateway: auth, model mapping and a memory guard in front of your local backend."
        },
        {
          "status": "beta",
          "text": "The headless agent and the `almanac` CLI over your own Markdown knowledge base, indexed with SQLite FTS5, with tools declared in TOML, confirmation before anything that changes the world, and an audit log. Replies stream, and the agent can use other MCP servers (such as XivMcp) while respecting their tiers."
        },
        {
          "status": "beta",
          "text": "Almanac as an MCP server over stdio and streamable HTTP, so Claude Code and Codex can use the same knowledge and tools. MCP elicitation has been tested only with the MCP SDK's own client."
        }
      ]
    }
  ]
}
